---
import Layout from '../layouts/Layout.astro';
---

<Layout title="solveOS — The first framework for humans and AI agents to solve problems together">

  <!-- ── Nav ──────────────────────────────────────────── -->
  <nav>
    <div class="container nav-inner">
      <a href="/" class="nav-logo">solveOS</a>
      <div class="nav-right">
        <a href="/docs/" class="nav-link">Docs</a>
        <a href="https://github.com/t0k1dev/solveOS/tree/main" class="nav-github" target="_blank" rel="noopener" aria-label="GitHub repository">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 0C5.374 0 0 5.373 0 12c0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0 1 12 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"/></svg>
        </a>
      </div>
    </div>
  </nav>

  <!-- ── Section 1: Hero ──────────────────────────────── -->
  <section id="hero" class="hero">
    <div class="container hero-inner">
      <p class="hero-eyebrow">Problem-solving framework</p>
      <h1 class="hero-title">The first framework for humans and AI agents to solve problems together.</h1>
      <p class="hero-sub">
        Plan. Build. Ship. A repeatable structure that tells you what to do, in what order, and who is responsible for each decision — whether you're working alone, with a team, or with AI doing most of the execution.
      </p>
      <p class="hero-agnostic">
        Works with any AI model. Applies to any type of problem — software, content, product, research, or anything else you build and ship.
      </p>

      <div class="loop-diagram">
        <div class="loop-main">
          <span class="loop-arrow-left">↻</span>
          <div class="loop-steps">
            <span class="lph">Plan</span>
            <span class="lar">→</span>
            <span class="lph">Build</span>
            <span class="lar">→</span>
            <span class="lph">Ship</span>
            <span class="lar">→</span>
            <span class="lgt">[Review]</span>
            <span class="lar">→</span>
            <span class="lph">Plan</span>
            <span class="lar">···</span>
          </div>
        </div>
        <div class="loop-labels">
          <div class="loop-label"><span class="ll-name">Plan</span><span class="ll-desc">Define the problem and write the brief before touching anything</span></div>
          <div class="loop-label"><span class="ll-name">Build</span><span class="ll-desc">Execute against the brief; update it when the build teaches you something</span></div>
          <div class="loop-label"><span class="ll-name">Ship</span><span class="ll-desc">Put it in front of real people — not "ready to ship", shipped</span></div>
          <div class="loop-label"><span class="ll-name ll-gate">[Review]</span><span class="ll-desc">Measure what happened; feed what you learned into the next cycle</span></div>
        </div>
      </div>

      <div class="hero-ctas">
        <a href="/docs/" class="btn btn-primary">Read the docs</a>
        <a href="https://github.com/t0k1dev/solveOS/tree/main" class="btn btn-ghost" target="_blank" rel="noopener">View on GitHub</a>
      </div>
    </div>
  </section>

  <!-- ── Section 2: The problem ───────────────────────── -->
  <section id="why" class="alt">
    <div class="container">
      <span class="label">Why solveOS</span>
      <h2 class="section-title">Most people fail at solving things for the same four reasons.</h2>
      <p class="section-sub">You have probably experienced at least one of these.</p>

      <div class="grid-2 failure-grid">
        <div class="failure-card">
          <div class="failure-num">01</div>
          <h3>They skip planning.</h3>
          <p>They jump into execution without a clear goal and end up building the wrong thing. The work is real — the output isn't useful.</p>
        </div>
        <div class="failure-card">
          <div class="failure-num">02</div>
          <h3>They over-plan.</h3>
          <p>They get stuck refining the plan and never ship anything real. Perfect plans that never touch the world produce nothing.</p>
        </div>
        <div class="failure-card">
          <div class="failure-num">03</div>
          <h3>They lose direction.</h3>
          <p>The goal defined at the start gets distorted by the time execution ends. By the time it ships, nobody remembers what problem it was solving.</p>
        </div>
        <div class="failure-card">
          <div class="failure-num">04</div>
          <h3>They never measure.</h3>
          <p>They ship something, move on, and never find out if it worked. The same mistakes repeat in the next cycle, invisibly.</p>
        </div>
      </div>

      <p class="why-close">
        Here's how the framework is structured.
      </p>
    </div>
  </section>

  <!-- ── Section 3: How it works ──────────────────────── -->
  <section id="phases">
    <div class="container">
      <span class="label">The framework</span>
      <h2 class="section-title">Three phases. Always in this order.</h2>
      <p class="section-sub">You plan before you build. You build before you ship. The loop closes when you review.</p>

      <div class="grid-3">

        <div class="phase-card card">
          <div class="phase-number">01</div>
          <h3>Plan</h3>
          <p class="phase-desc">Define the problem, audience, goal, and success criteria before you build anything.</p>
          <p class="phase-note">Output: a Plan Brief — the instruction set for the Build phase and the primary input for AI execution.</p>
          <span class="card-link card-link-disabled">Plan phase <span class="cs-badge">coming soon</span></span>
        </div>

        <div class="phase-card card">
          <div class="phase-number">02</div>
          <h3>Build</h3>
          <p class="phase-desc">Execute against the plan. Stay anchored. Update the brief when the build teaches you something.</p>
          <p class="phase-note">Build is structured discovery. You will learn things that planning couldn't reveal — the brief is your anchor when you do.</p>
          <span class="card-link card-link-disabled">Build phase <span class="cs-badge">coming soon</span></span>
        </div>

        <div class="phase-card card">
          <div class="phase-number">03</div>
          <h3>Ship</h3>
          <p class="phase-desc">Put it in the world. In front of real people. Not "ready to ship" — shipped.</p>
          <p class="phase-note">Until something ships, you are operating on assumptions. Reality is the only validator. Ship, then review.</p>
          <span class="card-link card-link-disabled">Ship phase <span class="cs-badge">coming soon</span></span>
        </div>

      </div>

      <div class="gates-note">
        Optional gates — [Research], [Plan Validation], [Build Validation], [Review] — sit between phases. Use them when the cost of being wrong is high. Skip them when it isn't.
        <span class="card-link card-link-disabled">Gates overview <span class="cs-badge">coming soon</span></span>
      </div>
    </div>
  </section>

  <!-- ── Section 4: Built for AI ──────────────────────── -->
  <section id="ai" class="ai-dark">
    <div class="container ai-outer">
      <div class="ai-text">
        <span class="label">AI-first design</span>
        <h2 class="section-title">AI executes. The human decides.</h2>
        <p class="ai-body">
          Most frameworks were designed before AI could do meaningful work. solveOS is built from the ground up assuming AI can own the Build phase.
        </p>
        <p class="ai-body">
          The Plan Brief is the human's primary output — a written document specific enough that an AI agent can execute it without clarifying questions. At every gate, the human reads the output and makes the call: proceed, iterate, or cut. AI does not decide what gets shipped. That call belongs to the human at every step.
        </p>
        <p class="ai-body">
          AI also has no memory between sessions. solveOS treats context as a first-class artifact. Every phase transition requires carrying forward what was learned — in writing — so the next session doesn't start from zero.
        </p>
        <span class="btn btn-outline btn-disabled">How AI fits into solveOS <span class="cs-badge">coming soon</span></span>
      </div>
      <div class="ai-summary">
        <div class="ai-row">
          <div class="ai-role-label">AI can</div>
          <ul class="ai-list">
            <li>Challenge the problem statement and sharpen the plan</li>
            <li>Produce code, text, designs, and other output given a clear brief</li>
            <li>Check output against criteria and flag gaps</li>
            <li>Summarize, measure, and surface patterns after Ship</li>
          </ul>
        </div>
        <div class="ai-divider"></div>
        <div class="ai-row">
          <div class="ai-role-label ai-role-human">Human must</div>
          <ul class="ai-list">
            <li>Own the goal and decide what success means</li>
            <li>Commit to the plan — in writing</li>
            <li>Recognize when output doesn't match the goal</li>
            <li>Decide that this goes in front of real people</li>
            <li>Decide what to learn and what to change next cycle</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- ── Section 5: The cycle ─────────────────────────── -->
  <section id="cycle">
    <div class="container">
      <span class="label">The loop</span>
      <h2 class="section-title">It's a cycle, not a line.</h2>
      <p class="section-sub">
        Without a loop before Build, you build the wrong thing. Without a loop after Ship, you repeat the same mistakes. These two loops close both gaps.
      </p>

      <div class="grid-2 cycle-grid">
        <div class="cycle-item card">
          <div class="cycle-num">Loop 1</div>
          <h3>Refine before you build.</h3>
          <div class="cycle-flow">
            <span class="lph-sm">Plan</span>
            <span class="lar-sm">→</span>
            <span class="lgt-sm">[Plan Validation]</span>
            <span class="lar-sm">→ gaps found →</span>
            <span class="lph-sm">Plan</span>
            <span class="lar-sm">···</span>
          </div>
          <p>Plan Validation checks the plan before any build work begins. If gaps exist, you go back to Plan to sharpen it. The loop runs until the plan can be executed without guesswork — typically 1–3 passes. Fixing ambiguity before execution is cheaper than fixing it after.</p>
        </div>
        <div class="cycle-item card">
          <div class="cycle-num">Loop 2</div>
          <h3>Keep building after you ship.</h3>
          <div class="cycle-flow">
            <span class="lph-sm">Ship</span>
            <span class="lar-sm">→</span>
            <span class="lgt-sm">[Review]</span>
            <span class="lar-sm">→</span>
            <span class="lph-sm">Plan</span>
            <span class="lar-sm">→</span>
            <span class="lph-sm">···</span>
          </div>
          <p>Shipping is the beginning of knowing, not the end of the work. Review captures what worked, what didn't, and what you discovered — and feeds that directly into the next Plan. Every cycle starts smarter than the last.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ── Section 6: Notify ─────────────────────────────── -->
  <section id="notify" class="alt">
    <div class="container notify-inner">
      <h2 class="notify-title">Get notified when docs and tools launch.</h2>
      <p class="notify-sub">Docs, worked examples, Plan Brief templates, and the CLI — one email when they're ready.</p>
      <form class="notify-form" onsubmit="return false;">
        <input
          type="email"
          class="notify-input"
          placeholder="your@email.com"
          aria-label="Email address"
          disabled
        />
        <button type="submit" class="btn btn-primary notify-btn btn-disabled" disabled>Notify me <span class="cs-badge cs-badge-inv">coming soon</span></button>
      </form>
      <p class="notify-note">Nothing ships until it's ready. That's the whole point.</p>
    </div>
  </section>

  <!-- ── Footer ────────────────────────────────────────── -->
  <footer>
    <div class="container footer-inner">
      <span class="footer-logo">solveOS</span>
      <span class="footer-tagline">A living framework. It improves through use.</span>
      <span class="footer-credit">Built with ♥ by @tokidev_</span>
      <a href="https://github.com/t0k1dev/solveOS/tree/main" class="footer-github" target="_blank" rel="noopener" aria-label="GitHub repository">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 0C5.374 0 0 5.373 0 12c0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0 1 12 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"/></svg>
        GitHub
      </a>
      <span class="footer-link footer-link-disabled">Full documentation <span class="cs-badge cs-badge-dark">coming soon</span></span>
    </div>
  </footer>

</Layout>

<style>
  /* ── Nav ─────────────────────────────── */
  nav {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(10, 10, 10, 0.95);
    backdrop-filter: blur(8px);
    border-bottom: 1px solid rgba(255,255,255,.08);
  }
  .nav-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 3.25rem;
  }
  .nav-logo {
    font-size: 0.95rem;
    font-weight: 700;
    color: #fff;
    letter-spacing: -0.01em;
  }
  .nav-logo:hover { text-decoration: none; opacity: 0.8; }
  .nav-right {
    display: flex;
    align-items: center;
    gap: 1.25rem;
  }
  .nav-link {
    font-size: 0.85rem;
    color: rgba(255,255,255,.55);
  }
  .nav-coming-soon { cursor: default; }
  .nav-github {
    display: flex;
    align-items: center;
    color: rgba(255,255,255,.55);
    transition: color 0.15s;
  }
  .nav-github:hover { color: #fff; text-decoration: none; }

  /* ── Hero ───────────────────────────── */
  .hero {
    background: var(--bg-hero);
    color: var(--text-inv);
    padding: 6rem 0 5rem;
  }
  .hero-inner {
    max-width: 780px;
  }
  .hero-eyebrow {
    font-size: 0.78rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 1.25rem;
  }
  .hero-title {
    font-size: clamp(2.2rem, 5vw, 3.6rem);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.03em;
    color: #fff;
    margin-bottom: 1.25rem;
  }
  .hero-sub {
    font-size: 1.1rem;
    color: var(--text-inv-muted);
    max-width: 600px;
    line-height: 1.75;
    margin-bottom: 1rem;
  }
  .hero-agnostic {
    font-size: 0.9rem;
    color: rgba(255,255,255,.4);
    max-width: 560px;
    line-height: 1.65;
    margin-bottom: 2.5rem;
  }
  .loop-diagram {
    margin-bottom: 2.5rem;
    padding: 1.25rem 0;
    border-top: 1px solid rgba(255,255,255,.1);
    border-bottom: 1px solid rgba(255,255,255,.1);
  }
  .loop-main {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
  }
  .loop-steps {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
    font-family: var(--font-mono);
    font-size: 1rem;
  }
  .lph  { color: #60a5fa; font-weight: 700; }
  .lgt  { color: #94a3b8; }
  .lar  { color: rgba(255,255,255,.25); }
  .loop-labels {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
    margin-top: 1.25rem;
    padding-top: 1.25rem;
    border-top: 1px solid rgba(255,255,255,.08);
  }
  .loop-label { display: flex; flex-direction: column; gap: 0.3rem; }
  .ll-name {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 700;
    color: #60a5fa;
  }
  .ll-gate { color: #94a3b8; }
  .ll-desc {
    font-size: 0.75rem;
    color: rgba(255,255,255,.38);
    line-height: 1.5;
  }
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  /* ── Failure cards ──────────────────── */
  .failure-grid { margin-bottom: 2rem; }
  .failure-card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 1.5rem;
    border-left: 3px solid var(--warn-color);
    box-shadow: var(--shadow);
  }
  .failure-num {
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    color: var(--text-muted);
    margin-bottom: 0.5rem;
    font-family: var(--font-mono);
  }
  .failure-card h3 {
    font-size: 1rem;
    margin-bottom: 0.4rem;
    color: var(--text);
  }
  .failure-card p {
    font-size: 0.875rem;
    color: var(--text-muted);
    line-height: 1.6;
  }
  .why-close {
    font-size: 1rem;
    color: var(--text-muted);
    max-width: 680px;
    line-height: 1.7;
    padding-top: 0.5rem;
  }

  /* ── Phase cards ────────────────────── */
  .phase-card {
    display: flex;
    flex-direction: column;
  }
  .phase-number {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 700;
    color: var(--text-muted);
    margin-bottom: 0.75rem;
    letter-spacing: 0.05em;
  }
  .phase-card h3 {
    font-size: 1.2rem;
    color: var(--phase-color);
    margin-bottom: 0.4rem;
  }
  .phase-desc {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 0.6rem;
    line-height: 1.5;
  }
  .phase-note {
    font-size: 0.85rem;
    color: var(--text-muted);
    line-height: 1.65;
    flex: 1;
    margin-bottom: 0.5rem;
  }
  .gates-note {
    margin-top: 2.5rem;
    padding: 1.25rem 1.5rem;
    background: var(--bg-alt);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.65;
    display: flex;
    align-items: baseline;
    gap: 1.5rem;
    flex-wrap: wrap;
  }
  .gates-note .card-link-disabled {
    margin-top: 0;
    padding-top: 0;
    white-space: nowrap;
  }

  /* ── AI section ─────────────────────── */
  .ai-dark {
    background: #0f172a;
    color: var(--text-inv);
  }
  .ai-dark .section-title { color: #fff; }
  .ai-dark .label { color: #60a5fa; }
  .ai-outer {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }
  .ai-body {
    font-size: 0.95rem;
    color: rgba(255,255,255,.6);
    line-height: 1.75;
    margin-bottom: 1rem;
  }
  .ai-summary {
    background: rgba(255,255,255,.05);
    border: 1px solid rgba(255,255,255,.1);
    border-radius: var(--radius-lg);
    padding: 1.5rem;
  }
  .ai-row { padding: 0.5rem 0; }
  .ai-role-label {
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #94a3b8;
    margin-bottom: 0.75rem;
    padding: 0.2em 0.6em;
    background: rgba(148,163,184,.12);
    border-radius: 3px;
    display: inline-block;
  }
  .ai-role-human {
    color: #60a5fa;
    background: rgba(96,165,250,.12);
  }
  .ai-divider {
    height: 1px;
    background: rgba(255,255,255,.1);
    margin: 1rem 0;
  }
  .ai-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .ai-list li {
    font-size: 0.875rem;
    color: rgba(255,255,255,.5);
    line-height: 1.55;
    padding-left: 1rem;
    position: relative;
  }
  .ai-list li::before {
    content: '–';
    position: absolute;
    left: 0;
    color: rgba(255,255,255,.2);
  }
  .ai-dark .btn-outline {
    color: #60a5fa;
    border-color: rgba(96,165,250,.4);
  }

  /* ── Cycle cards ────────────────────── */
  .cycle-grid { align-items: start; }
  .cycle-item { padding: 1.75rem; }
  .cycle-num {
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text-muted);
    margin-bottom: 0.5rem;
  }
  .cycle-item h3 {
    font-size: 1.05rem;
    margin-bottom: 0.75rem;
    color: var(--text);
  }
  .cycle-flow {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.3rem;
    font-family: var(--font-mono);
    font-size: 0.78rem;
    margin-bottom: 1rem;
    padding: 0.6rem 0.75rem;
    background: var(--bg-alt);
    border: 1px solid var(--border);
    border-radius: var(--radius);
  }
  .lph-sm { color: var(--phase-color); font-weight: 700; }
  .lgt-sm { color: var(--gate-color); }
  .lar-sm { color: var(--border); }
  .cycle-item > p {
    font-size: 0.875rem;
    color: var(--text-muted);
    line-height: 1.65;
  }

  /* ── Coming Soon ─────────────────────── */
  .cs-badge {
    display: inline-block;
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    background: var(--bg-alt);
    color: var(--text-muted);
    border: 1px solid var(--border);
    border-radius: 3px;
    padding: 0.1em 0.4em;
    vertical-align: middle;
    margin-left: 0.35em;
    line-height: 1.4;
  }
  .cs-badge-inv {
    background: rgba(255,255,255,.12);
    color: rgba(255,255,255,.6);
    border-color: rgba(255,255,255,.2);
  }
  .cs-badge-dark {
    background: rgba(255,255,255,.08);
    color: rgba(255,255,255,.4);
    border-color: rgba(255,255,255,.12);
  }
  .btn-disabled {
    opacity: 0.5;
    cursor: default;
    pointer-events: none;
  }
  .card-link-disabled {
    display: inline-block;
    margin-top: auto;
    padding-top: 1rem;
    font-size: 0.875rem;
    color: var(--text-muted);
    opacity: 0.6;
    cursor: default;
  }

  /* ── Email notify ────────────────────── */
  .notify-inner {
    max-width: 560px;
    margin: 0 auto;
    text-align: center;
  }
  .notify-title {
    font-size: clamp(1.4rem, 3vw, 1.9rem);
    font-weight: 700;
    letter-spacing: -0.02em;
    margin-bottom: 0.5rem;
    color: var(--text);
  }
  .notify-sub {
    font-size: 0.95rem;
    color: var(--text-muted);
    margin-bottom: 1.5rem;
  }
  .notify-form {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    justify-content: center;
  }
  .notify-input {
    flex: 1;
    min-width: 220px;
    padding: 0.65rem 1rem;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: var(--card-bg);
    color: var(--text);
    font-size: 0.9rem;
    font-family: inherit;
    outline: none;
    transition: border-color 0.15s;
  }
  .notify-input:focus { border-color: var(--accent); }
  .notify-input:disabled { opacity: 0.45; cursor: not-allowed; }
  .notify-input::placeholder { color: var(--text-muted); opacity: 0.6; }
  .notify-btn { white-space: nowrap; }
  .notify-note {
    margin-top: 1rem;
    font-size: 0.8rem;
    color: var(--text-muted);
    opacity: 0.7;
    font-style: italic;
  }

  /* ── Footer ─────────────────────────── */
  footer {
    background: var(--bg-hero);
    padding: 2rem 0;
  }
  .footer-inner {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    flex-wrap: wrap;
  }
  .footer-logo {
    font-weight: 700;
    font-size: 0.9rem;
    color: #fff;
  }
  .footer-tagline {
    font-size: 0.825rem;
    color: rgba(255,255,255,.4);
    flex: 1;
  }
  .footer-credit {
    font-size: 0.825rem;
    color: rgba(255,255,255,.35);
  }
  .footer-github {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.825rem;
    color: rgba(255,255,255,.45);
    transition: color 0.15s;
  }
  .footer-github:hover { color: #fff; text-decoration: none; }
  .footer-link-disabled {
    font-size: 0.825rem;
    color: rgba(255,255,255,.25);
    cursor: default;
  }

  /* ── Responsive ─────────────────────── */
  @media (max-width: 900px) {
    .ai-outer { grid-template-columns: 1fr; gap: 2.5rem; }
    .loop-labels { grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 768px) {
    .hero { padding: 4rem 0 3.5rem; }
  }
  @media (max-width: 600px) {
    .gates-note { flex-direction: column; gap: 0.75rem; }
    .loop-labels { grid-template-columns: 1fr 1fr; gap: 0.75rem; }
    .footer-tagline { display: none; }
    .footer-inner { gap: 1rem; }
  }
  @media (max-width: 400px) {
    .loop-labels { grid-template-columns: 1fr; }
  }
</style>
